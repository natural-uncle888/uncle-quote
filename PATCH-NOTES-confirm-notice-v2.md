# 報價確認注意事項修正 v2

更新日期：2026-07-16

## 已修正

1. 顧客確認報價後的注意事項彈窗不再倒數或自動關閉。
2. 彈窗只能由顧客按「我已閱讀並關閉」或鍵盤 Esc 主動關閉。
3. 長內容改為彈窗內捲動，手機版底部關閉按鈕固定保留在可操作區域。
4. 依報價項目的「服務名稱 + 規格」自動辨識並顯示：
   - 冷氣清洗
   - 洗衣機清洗
   - 直立式洗衣機清洗
   - 水塔清洗
   - 自來水管清洗
5. 支援冷氣／空調、分離式／吊隱式、水管清洗等常見名稱與別名。
6. 多種服務同時存在時，會合併顯示所有相關注意事項。
7. 無法辨識的服務會顯示通用行前提醒，不會出現空白彈窗。
8. 更新 PWA 快取版本，`app.js` 改為 network-first，避免手機繼續執行舊版 3 秒關閉程式。
9. `index.html` 使用版本化的 `/app.js?v=20260716-confirm-notice-v2`。
10. Netlify 對 `app.js` 與 `service-worker.js` 增加禁止長期快取的回應標頭。

## 部署

將此資料夾完整部署到 Netlify，不要只上傳 `app.js`，因為本次同時修改：

- `app.js`
- `index.html`
- `service-worker.js`
- `netlify.toml`

部署完成後，曾經開啟過網站的手機請重新整理一次，或完全關閉已安裝的 PWA 再重新開啟。新版網址參數與 Service Worker 會接手清除舊快取。

## 已執行測試

- 全部 JavaScript 檔案通過 `node --check`。
- 注意事項辨識測試：冷氣、直立式洗衣機、冷氣防霉別名、水管別名、水塔、多服務、未知服務。
- 程式碼確認無確認彈窗的 3 秒計時或自動重新整理。
